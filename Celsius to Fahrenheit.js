// fahrenheit = celsius * 1.8 + 32

const a = prompt('Enter the Celsius value to convert : ');

const factor = (a * 1.8) + 32;
console.log(`${a} Celsius is equals to ${factor} Fahrenheit`);

// celsius = (fahrenheit - 32) / 1.8