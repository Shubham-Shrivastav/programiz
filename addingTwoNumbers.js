const num1 = parseInt(prompt('Enter the fist element '));
const num2 = parseInt(prompt('Enter the second element to add '));
const sum = num1 + num2;
console.log(`the added result of ${num1} + ${num2} is ${sum}`);

// this programs take user input and reply the added elements..